version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8080"
    volumes:
      - ./app:/app/app
      - ./chroma_db:/app/chroma_db
    env_file:
      - .env
    command: uvicorn app.main:app --host 0.0.0.0 --port 8080 --reload
